from langchain_core.prompts import ChatPromptTemplate
from backend.llm import llm

DATA_ANALYST_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是资深股票数据分析师。必须用 Markdown 格式输出，让用户一眼看懂。

【硬性规则 - 违反将导致格式错误】
1. 标题独占一行：## 或 ### 后面只能写标题文字（不超过15个字），写完后立刻换行。
   正确：## 趋势分析\\n\\n
   错误：## 趋势分析茅台在...   （标题与正文合并）

2. 每个 ## / ### 标题后必须有一个空行，再写正文。

3. 正文中的每条要点独占一行，用 -  开头。
   正确：
   - 最高价 **100元**
   - 最低价 **80元**
   错误：- 最高价 **100元**- 最低价 **80元**（多个要点挤在一行）

4. 表格前后必须各空一行，表头分隔行不可省略。
   正确：
   | 日期 | 收盘价 |
   | --- | --- |
   | 2025-01-02 | **100.00元** |

5. 涨跌或正负值末尾加 ↑（上涨/正值）或 ↓（下跌/负值）

6. 最后一段用 > 引用格式给出简练总结

【数值规则】
- 价格保留两位小数并加粗，如 **1130.00元**
- 涨跌幅保留两位小数带%号，如 **-3.56%**
- 涨跌额保留两位小数带元，如 **-53.01元**

【字段说明】
- trade_date: 交易日期 (YYYY-MM-DD)
- open/high/low/close: 开/高/低/收盘价 (元)
- pct_chg: 涨跌幅 (%)
- change: 涨跌额 (元)
- vol: 成交量 (手)
- amount: 成交额 (千元)

分析指令：{analysis_instruction}

统计摘要：{stats}

输出示例（严格模仿此格式）：

## 分析结论

贵州茅台在 2020-01-02 至 2020-02-03 期间：

- 最高收盘价出现在 **1月2日**，为 **1130.00元**
- 最低收盘价出现在 **2月3日**，为 **1003.92元** ↑

> 整体呈先涨后跌的震荡走势，区间波动 **126.08元**"""),
    ("human", """用户问题：{question}

SQL：{sql}

查询结果：
{data}"""),
])

analyst_chain = DATA_ANALYST_PROMPT | llm

ANALYSIS_INSTRUCTIONS = {
    'trend': """这是趋势/走势分析：
- 描述整体趋势方向（上升/下降/震荡）
- 列出起始值、结束值和变化幅度
- 找出阶段高点和低点
- 计算整体波动率或涨跌幅区间
- 给出简短的趋势评价""",
    'compare': """这是对比分析：
- 列出每只股票的核心指标排名
- 指出表现最好和最差的股票
- 计算各股票间的差异幅度
- 给出简短的综合评价""",
    'extreme': """这是极值查询：
- 直接给出最大值/最小值及其对应日期
- 如有多条记录，列出前3-5条
- 给出极值出现的背景说明""",
    'statistics': """这是统计分析：
- 列出核心统计指标（均值、中位数、标准差）
- 分析数据分布特征
- 如有明显异常值，指出并说明
- 给出统计意义上的结论""",
    'evaluate': """这是综合评价分析：
- 从多维度评价股票表现（价格、涨跌幅、成交量等）
- 给出好/中/差的评级和理由
- 如有明显趋势或异常，重点说明
- 给出参考建议""",
    'simple': """这是简单数据查询：
- 直接回答用户问题
- 给出关键数值
- 不需要过度分析""",
}
